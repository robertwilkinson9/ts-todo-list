const mongoose = require('mongoose')
const Schema = mongoose.Schema

const Todo = new Schema(
    {
        due: Date,
        summary: String, 
        text: String,
    },
)

const mytodo = mongoose.model('todo', Todo);
console.log("Got mytodo");
//module.exports = mongoose.model('todo', Todo)
module.exports = mytodo
